@{
    ViewData["Title"] = "Home Page";
}

@using System.Security.Claims

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p><a href="@Url.Action("CallApi","Home")" class="btn btn-lg btn-group btn-info">Get Product From API</a></p>

    @*<label>
        @User.Claims.Where(a => a.Type == ClaimTypes.NameIdentifier).FirstOrDefault()?.Value
</label>*@
</div>
